# Database Connection (MS SQL Server)
# Format: mssql+pyodbc://USER:PASSWORD@SERVER/DATABASE?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes
# Note: Encode special characters in password (e.g., & -> %26)
DB_CONN_STR=mssql+pyodbc://sa:YOUR_PASSWORD_HERE@YOUR_SERVER\\SQL/cdn_test?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes

# Google Gemini API Key (get from https://aistudio.google.com/)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# OpenRouter API Key (get from https://openrouter.ai/keys)
OPENROUTER_API_KEY=YOUR_OPENROUTER_API_KEY_HERE

# Ollama Configuration (Local LLM Server)
OLLAMA_HOST=http://localhost:11434

# =============================================================================
# LOCAL LLM CONFIGURATION (Embedded - No external server required)
# =============================================================================
# Path to GGUF model file for embedded LLM inference
# Recommended models (Dec 2024):
#   - Qwen2.5 3B Instruct (~1.96 GB): BEST - Latest model with 32k context
#   - Phi-3 Mini 4K Instruct (~2.2 GB): Fast, efficient
#   - Qwen2 1.5B Instruct (~1.1 GB): Smallest, very fast
#
# Download from HuggingFace and place in models/ folder
# Example: LOCAL_LLM_PATH=models/qwen2.5-3b-instruct-q4_k_m.gguf
LOCAL_LLM_PATH=

# Optional: Override number of CPU threads (default: auto-detect)
# LOCAL_LLM_THREADS=4

# Optional: Context window size (default: 2048)
# LOCAL_LLM_CONTEXT=2048

# =============================================================================
# SECURITY CONFIGURATION (Production Hardening)
# =============================================================================

# Encryption Master Key for sensitive data encryption
# Generate with: python -m src.security.encryption generate-key
# ENCRYPTION_MASTER_KEY=

# Optional: Custom salt for encryption (unique per installation)
# ENCRYPTION_SALT=

# Optional: Encrypted connection string (alternative to plain DB_CONN_STR)
# Encrypt with: python -m src.security.encryption encrypt "your_conn_string"
# ENCRYPTED_DB_CONN_STR=

# Optional: Audit log file path (default: logs/security_audit.log)
# AUDIT_LOG_PATH=logs/security_audit.log
